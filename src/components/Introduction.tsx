import React from 'react';
import { ourData } from '../libs/data';

const Introduction = () => {
    console.log(ourData);
    return (
        <div>
            
        </div>
    );
};

export default Introduction;